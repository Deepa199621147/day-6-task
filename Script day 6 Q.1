Typescript oops practice class movie?

oops :Object-oriented programming 
     
     Is a concept build into many languages that help associate date and 
     methods together in a single construct.

a. write a construct for the class movie, which takes a string represnding the title of
    the movie,an int represending the year the movie was released,and a double represending
   the rating as its arguments,and sets the respective class variable to these values.

b. write a second construct for the class movie,which takes a stringr represending the title of the 
   movie,and an int represending the release year,and sets the respective class variableto these
   values, which the class rating is set to 0.