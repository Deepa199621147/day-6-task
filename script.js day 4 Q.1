Do the below in anonymous function & IIFE
A.print odd numbers in an array
    var add =function (a,b){
        return a+b;
}
console.log(add(2,3));

    
B : Conver all the string to title caps in a string array?
function titlecase(str) {
    return str.toLowercase().split.('').map(function(word))
}).join('');

}
console.log (titlecase ("converting"))

 C :sum of all numbers in an array?
       
       int Main (){
        // intialise array
        int arr[] ={2,4,6,8};
        int size = 4;

        // initialise sum to zero 
        int sum = 0;
        // use function
        sum = accumulate(arr,arr+size,sum);
        count <<"The sum of the elements in the array:"<< sum;
       }
 
       D:   Return all the prime numbers in an array?

           var numbers =7 var count =0;
           for (var i=1;i<=>;i++)
           count ++;
        if(count==2)
        print prime 
        else
        print Composite 


        E: Return all the palindroms in an array?    
           console.log(palindromsarry([1,2,2,2]));
           console.log(palindroms([1,2,2,2,]));
           true and false

F :      Return median of two sorted arrays of the same size?

          input : ar1[] = {1,12,15,26,38}
                  ar2[] = {2,13,17,30,45}
           output:  16
           After merging two arrays,we get 
           {1,2,13,15.17,26,30,38,45}
           middle two Elements are 15,17 average
           of middle Elements is (15 +17)/2
           which is equal to 16.








